# ── Credenciales para Docker Compose ─────────────────────────
# Estos valores se usan por docker-compose.yml como defaults.
# Crea un archivo .env en esta misma carpeta (raíz del proyecto)
# para sobreescribir los valores por defecto.

# MongoDB
MONGO_USERNAME=admin
MONGO_PASSWORD=sharenotes123
